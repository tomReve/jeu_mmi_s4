{% extends 'admin.html.twig' %}

{% block title %}New Partie{% endblock %}

{% block body %}
    <div class="jumbotron" style="margin-bottom: 0">
        <h1>Ajouter une partie</h1>

        {{ include('partie/_form.html.twig') }}

        <br/>
        <a href="{{ path('partie_index') }}"><button class="btn btn-primary">Retourner à la liste</button></a>
    </div>
{% endblock %}
